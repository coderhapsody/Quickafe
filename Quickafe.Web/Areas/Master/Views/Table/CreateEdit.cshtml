@model Quickafe.Providers.ViewModels.Table.CreateEditViewModel

<div style="width: 550px;">
    @using (Html.BeginForm(null, null, FormMethod.Post, new { id = "addEditForm", name = "addEditForm" }))
    {
        @Html.HiddenFor(m => m.Id)
        @FormHelper.RenderAuditFields(Model)
        <div class="form-horizontal">
            <div class="form-group">
                <div class="col-md-2">@Html.LabelFor(m => m.Code): </div>
                <div class="col-md-7">
                    @Html.Kendo().TextBoxFor(m => m.Code).HtmlAttributes(new { style = "width:400px;" })
                </div>
            </div>

            <div class="form-group">
                <div class="col-md-2"></div>
                <div class="col-md-7">
                    @Html.Kendo().CheckBoxFor(m => m.IsActive)
                </div>
            </div>
            <div class="form-group">
                <div class="col-md-2"></div>
                <div class="col-md-7">
                    <button type="submit" id="btnSave" name="btnSave" class="k-button">@Strings.SaveButtonText</button>
                    <button type="button" id="btnClose" name="btnClose" class="k-button">@Strings.CloseButtonText</button>
                </div>
            </div>
        </div>
    }
</div>
<script>
    $(function () {
        $("#btnClose").click(Table.closeClick);
        $("#btnSave").click(Table.saveClick);
        $("#Code").focusEnd();
    });
</script>